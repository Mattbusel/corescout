[package]
name = "corescout-capability-runtime"
description = "Learned procedures as verified, bounded, reversible capabilities, and the runtime that executes them."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
corescout-permissions = { workspace = true }
corescout-agent-observation = { workspace = true }
