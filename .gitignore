/target
**/*.rs.bk
*.pdb

# Frontend build output and dependencies
node_modules/
dist/
.next/
out/
apps/corescout-desktop/dist/
apps/corescout-desktop/src-tauri/target/
apps/corescout-desktop/src-tauri/gen/
# Sidecar copies, produced by scripts/release.ps1 from target/release
apps/corescout-desktop/src-tauri/binaries/
web/website/.next/
web/website/out/
# Release artefacts, produced by scripts/release.ps1
dist/

# The signed-in browser profile the Partner Center automation uses, and the
# screenshots it takes while working. Both hold account session data.
microsoft-store/automation/.browser/
microsoft-store/automation/shots/
microsoft-store/automation/node_modules/
# A DOM dump the Partner Center automation writes while it works. Machine
# output, regenerated by discover.mjs, and it carries submission ids that have
# no business in a public repository.
microsoft-store/automation/discovered.json
